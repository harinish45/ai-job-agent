## 2026-05-15 - Scoping React Query Mutations in Lists\n**Learning:** When using React Query mutations inside a list of items, loading and disabled states must be scoped to the specific item by checking mutation.variables === item.id to avoid triggering global loading indicators across all list items.\n**Action:** Always verify that isPending and disabled states inside list maps check for the specific item ID when using a shared mutation.
